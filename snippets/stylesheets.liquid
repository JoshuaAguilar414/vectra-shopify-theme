{{ 'overflow-list.css' | asset_url | preload_tag: as: 'style' }}
{{ 'base.css' | asset_url | stylesheet_tag: preload: true }}

{%- if template.name == 'index' or template.suffix == 'our-story' -%}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&display=swap"
    rel="stylesheet"
  >
  {{ 'homepage.css' | asset_url | stylesheet_tag }}
{%- endif -%}
{%- if template.suffix == 'our-story' -%}
  {{ 'our-story.css' | asset_url | stylesheet_tag }}
{%- endif -%}
