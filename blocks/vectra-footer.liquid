{% comment %}
  The VECTRA footer is rendered once by snippets/vectra-page-shell.liquid
  so every VECTRA page uses the same module. This block stays in templates
  for the theme editor and does not print a second footer.
{% endcomment %}

{% schema %}
{
  "name": "VECTRA: Footer",
  "tag": null,
  "settings": [],
  "presets": [{ "name": "VECTRA: Footer" }]
}
{% endschema %}
