{% capture children %}
  {% content_for 'blocks' %}
{% endcapture %}
{% render 'vectra-page-shell', children: children, include_resources: true, skip_footer: true %}

{% schema %}
{
  "name": "VECTRA Contact",
  "class": "vectra-con-section",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "MEGA Navigation",
      "info": "Uses VECTRA Website Mega Menu (handle: vectra-website-mega-menu). Nested items render as header dropdowns."
    }
  ],
  "blocks": [
    { "type": "@theme" },
    { "type": "@app" }
  ],
  "presets": [
    {
      "name": "VECTRA Contact",
      "blocks": [
        { "type": "vectra-header" },
        { "type": "vectra-con-hero" },
        { "type": "vectra-con-challenges" },
        { "type": "vectra-con-next" },
        { "type": "vectra-con-direct" },
        { "type": "vectra-footer" }
      ]
    }
  ]
}
{% endschema %}
