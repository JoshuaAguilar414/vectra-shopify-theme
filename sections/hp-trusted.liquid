{% comment %}
  Trusted-by badge + CTA + logo marquee (Figma layout + motion).
{% endcomment %}

<div
  class="section-background hp-bg color-custom-{{ section.id }}"
  style="--hp-section-bg: {{ section.settings.background_color }};"
></div>
<div class="section section--full-width hp" id="trusted">
  <div class="hp-trusted" style="--hp-pad-y: {{ section.settings.padding_y }}px;">
    <div class="hp-trusted__bar">
      <homepage-reveal class="hp-trusted__badge-wrap" data-from="left" data-delay="1">
        {%- if section.settings.badge_image != blank -%}
          {{
            section.settings.badge_image
            | image_url: width: 400
            | image_tag: loading: 'lazy', class: 'hp-trusted__badge', alt: section.settings.badge_alt
          }}
        {%- else -%}
          <img
            class="hp-trusted__badge"
            src="{{ 'hp-trusted-badge.png' | asset_url }}"
            alt="{{ section.settings.badge_alt | escape }}"
            width="198"
            height="193"
            loading="lazy"
          >
        {%- endif -%}
      </homepage-reveal>

      <homepage-reveal class="hp-trusted__copy" data-from="right" data-delay="2">
        {%- if section.settings.heading != blank -%}
          <p class="hp-trusted__heading">{{ section.settings.heading | escape }}</p>
        {%- endif -%}
        {%- if section.settings.button_label != blank -%}
          <a class="hp-trusted__btn" href="{{ section.settings.button_link | default: '#marketplace' }}">
            {{ section.settings.button_label | escape }}
          </a>
        {%- endif -%}
      </homepage-reveal>
    </div>

    <div class="hp-trusted__marquee" aria-label="Trusted by">
      <div class="hp-trusted__track">
        {%- for i in (1..10) -%}
          <div class="hp-trusted__logo">
            <img src="{{ 'hp-logo-' | append: i | append: '.png' | asset_url }}" alt="" loading="lazy">
          </div>
        {%- endfor -%}
        {%- for i in (1..10) -%}
          <div class="hp-trusted__logo" aria-hidden="true">
            <img src="{{ 'hp-logo-' | append: i | append: '.png' | asset_url }}" alt="" loading="lazy">
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Homepage trusted bar",
  "tag": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Trusted Partnership. Practical Implementation."
    },
    { "type": "image_picker", "id": "badge_image", "label": "Badge image" },
    {
      "type": "text",
      "id": "badge_alt",
      "label": "Badge alt text",
      "default": "Trusted by 500+ Companies"
    },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Explore case studies→" },
    { "type": "url", "id": "button_link", "label": "Button link" },
    { "type": "color", "id": "background_color", "label": "Background", "default": "#ffffff" },
    {
      "type": "range",
      "id": "padding_y",
      "label": "Vertical padding",
      "min": 16,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 28
    }
  ],
  "presets": [{ "name": "Homepage trusted bar" }]
}
{% endschema %}
